```{r}
df <- read.csv("crime.csv", row.names="X")
df
```
```{r}
boxplot(df)
```
```{r}
X <- scale(df,center = FALSE, scale = TRUE) 
d <- dist(X)
```


```{r}
# Hierarchical clustering using Complete Linkage
hc1 <- hclust(d, method = "complete" )

# Plot the obtained dendrogram
plot(hc1, cex = 0.6, hang = -1)
abline(h = 2.0, col = "blue")
```
```{r}
library(dplyr)
```
```{r}
murd <- df %>% arrange(Murder)
murd
```
```{r}
rob <- df %>% arrange(Robbery)
rob
```


```{r}
hc2 <- agnes(X, method = "complete")
```
```{r}
m <- c( "average", "single", "complete", "ward")
names(m) <- c( "average", "single", "complete", "ward")
# function to compute coefficient
ac <- function(x) {
  agnes(X, method = x)$ac
}
map_dbl(m, ac)
```
```{r}
plot(hc2)
abline(h = 2.0, col = "red")
```

